var stuff = Array();

var things = $('such-text');

var box = $('saying');

box.change(see);

function see (doggo) { 
var words = box does val with ;
    
if (words  === 'doggo'  || words  === 'doge' ) {
var big = words.toUpperCase();
stuff.push(big);
} else {
stuff.push(words);
} 
    
var everything = stuff.join(' ');
things.html(everything);
    box.value = ''
} 
